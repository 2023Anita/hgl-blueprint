# Separate design from build

HGL Blueprint first produces a provider-neutral Blueprint in review state, then stops for explicit human approval before generating code or runnable workflow artifacts. This prevents implementation momentum from silently deciding scope, permissions, or completion criteria.

